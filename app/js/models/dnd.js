var model_dnd_tpl = function (title, description) {
    this.title = title;
    this.description = description;
}

var dnd_tpl = new model_dnd_tpl (
    "Drag and Drop example.",
    "Use it at your own risk!"
);
