var model_map_tpl = function(title, sizeX, sizeY) {
    this.title = title;
    this.sizeX = sizeX;
    this.sizeY = sizeY;
}

var map_tpl = new model_map_tpl(
    "Map", /* Title */
    10, /* sizeX */
    10 /* sizeY */
);
